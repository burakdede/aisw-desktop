export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">AISW Desktop</p>
        <h1>Desktop control plane for AI coding agent identities.</h1>
        <p className="lede">
          The implementation scaffold is in place. Backend bridge, workflows,
          and UI features are added in subsequent commits.
        </p>
      </section>
    </main>
  );
}
